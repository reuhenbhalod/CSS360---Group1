fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
python-dotenv>=1.0
pytest>=8.0
# Note: Reddit's bot filter rejects every Python HTTP client we tried
# (httpx, curl_cffi). The Reddit fetcher shells out to the system `curl`
# binary instead — no extra Python package needed.
